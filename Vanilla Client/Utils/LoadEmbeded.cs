using System.IO;
using System.Reflection;

namespace Vanilla.Utils
{
    internal class Testing
    {
        public static void Test()
        {
            //Console.WriteLine("AuthInfo");
            /*
            Console.WriteLine(VRC.Core.ApiCredentials.authToken);

            Console.WriteLine(VRC.Core.ApiCredentials.GetAuthToken());
            Console.WriteLine(VRC.Core.ApiCredentials.twoFactorAuthToken);
            Console.WriteLine(VRC.Core.ApiCredentials.GetTwoFactorAuthToken());
*/
        }
    }
}